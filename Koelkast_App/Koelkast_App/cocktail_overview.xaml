<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Koelkast_App"
             x:Class="Koelkast_App.cocktail_overview">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="Search:" Style="{StaticResource MidEndEntry}"/>
                <Entry x:Name="searchEntry" Placeholder="cocktail name"/>
                <Button x:Name="SeacrchButton" Text="Search" Clicked="SeacrchButton_Clicked"/>
            </StackLayout>
            <ListView x:Name="cocktailListView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Horizontal">
                            <Image Source="{Binding ImgSrc}"/>
                            <Label Text="{Binding Name}"/>
                        </StackLayout>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>